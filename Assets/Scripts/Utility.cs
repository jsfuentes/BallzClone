using UnityEngine;
using System.Collections;

public class Utility : MonoBehaviour{

    // The last element should always be NUM_ELEMENTS so that we can use this enum for array indexing and looping
    public enum ShapeTypes { SQUARE, TRIANGLE, PENTAGON, NUM_ELEMENTS };

}
